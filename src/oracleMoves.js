export const UP = Symbol("up");
export const MID = Symbol("MID");
export const DOWN = Symbol("DOWN");
export const NONE = Symbol("NONE");

// this is in its own file so we can mess with these values for the tests. It's not ideal.
